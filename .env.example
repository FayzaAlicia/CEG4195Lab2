# Application config
APP_HOST=0.0.0.0
APP_PORT=5000
IMAGE_SIZE=256
MASK_THRESHOLD=0.5
DEVICE=cpu

# Model loading
MODEL_PATH=artifacts/model_best.pth
STRICT_MODEL_LOAD=false

# Optional remote checkpoint download (kept secret in .env locally)
MODEL_URL=
HF_TOKEN=
